import 'package:flutter/material.dart';
import 'ChatScreen.dart';

class HodChatScreen extends StatelessWidget {
  const HodChatScreen({super.key});

  @override
  Widget build(BuildContext context) {

    return const ChatScreen(
      title: "HOD",
    );
  }
}